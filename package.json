{
	"name": "packages-maven-mirror",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"format": "prettier --write src/"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.10.0",
		"prettier": "^3.5.3",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.20.0"
	}
}
